package support

import (
	"errors"

	"github.com/bitwormhole/guilink4go/guilink/network/packetflow"
	"github.com/bitwormhole/guilink4go/guilink/vo"
)

func decodePacket(p1 *packetflow.Packet) (*vo.Packet, error) {

	return nil, errors.New("no impl")
}

func encodePacket(p1 *vo.Packet) (*packetflow.Packet, error) {

	return nil, errors.New("no impl")
}
